import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.moduleart.neutodo",
  appName: "NeuTodo",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
